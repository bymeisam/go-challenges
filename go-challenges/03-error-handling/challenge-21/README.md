# Challenge 21: Custom Error Types

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Create custom error types for better error handling.

## Tasks
1. Define `ValidationError` struct implementing `error` interface
2. `ValidateAge(age int) error` - return custom error
3. Check error type with type assertion

```bash
go test -v
```
