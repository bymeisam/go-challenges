# Challenge 22: Error Wrapping
**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Use `fmt.Errorf` with `%w` to wrap errors and maintain context.

## Tasks
1. `ReadFile(filename string) error` - wrap file errors
2. `errors.Is()` and `errors.As()` to check wrapped errors

```bash
go test -v
```
