# Challenge 50: Channel Direction

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Use directional channels for safer concurrent code.

## Tasks
1. Create send-only channels `chan<-`
2. Create receive-only channels `<-chan`
3. Understand channel direction in function signatures

```bash
go test -v
```
