# Challenge 52: Default Select

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Use default case in select for non-blocking operations.

## Tasks
1. Implement non-blocking send
2. Implement non-blocking receive
3. Use default case to avoid blocking

```bash
go test -v
```
