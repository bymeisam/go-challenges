# Challenge 07: Methods

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

## 🎯 Learning Goals
Learn to define methods on structs (Go's way of adding behavior to types)

## 🔨 Tasks
Define a `Rectangle` struct and implement:
1. `Area() float64` method
2. `Perimeter() float64` method
3. `Scale(factor float64)` method (pointer receiver to modify)

```bash
go test -v
```
