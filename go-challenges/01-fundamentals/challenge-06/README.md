# Challenge 06: Functions and Closures

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

## 🎯 Learning Goals
Multiple return values, closures, higher-order functions

## 🔨 Tasks
1. `Divide(a, b float64) (float64, error)` - safe division
2. `MakeMultiplier(factor int) func(int) int` - return a closure
3. `ApplyOperation(nums []int, op func(int) int) []int` - higher-order function

```bash
go test -v
```
