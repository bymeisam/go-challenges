# Challenge 05: Pointers

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

## 🎯 Learning Goals
Learn pointer basics, dereferencing, and when to use pointers vs values.

## 🔨 Tasks
1. `SwapValues(a, b *int)` - swap two integers using pointers
2. `DoubleValue(n *int)` - double the value via pointer
3. `GetPointer(x int) *int` - return pointer to integer

## 💡 Key Concept
Unlike JS (everything is reference), Go has explicit pointers. Use `&` for address, `*` to dereference.

```bash
go test -v
```
