# Challenge 35: io.Reader and io.Writer

**Difficulty:** ⭐⭐⭐ Hard | **Time:** 25 min

Master Go's most important interfaces from the standard library.

## Tasks
1. Implement `io.Reader` interface
2. Implement `io.Writer` interface  
3. Use `io.Copy()` to copy data between reader and writer

```bash
go test -v
```
