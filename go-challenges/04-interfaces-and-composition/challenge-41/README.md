# Challenge 41: Abstract Factory with Interfaces

**Difficulty:** ⭐⭐⭐ Hard | **Time:** 30 min

Use interfaces to implement the Abstract Factory pattern.

## Tasks
1. Create `Database` interface with `Connect()` and `Query()` methods
2. Implement `MySQL` and `PostgreSQL` types
3. Create factory function that returns appropriate database

```bash
go test -v
```
