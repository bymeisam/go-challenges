# Challenge 36: Sort Interface

**Difficulty:** ⭐⭐⭐ Hard | **Time:** 20 min

Implement sort.Interface to sort custom types.

## Tasks
1. Create a custom type (e.g., Person slice)
2. Implement `Len()`, `Less()`, `Swap()` methods
3. Use `sort.Sort()` to sort your custom type

```bash
go test -v
```
