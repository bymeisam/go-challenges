# Challenge 38: Interface Polymorphism

**Difficulty:** ⭐⭐⭐ Hard | **Time:** 25 min

Use interfaces for polymorphism - multiple types, one interface.

## Tasks
1. Create `Logger` interface with `Log(string)` method
2. Implement `FileLogger` and `ConsoleLogger`
3. Use both through the Logger interface

```bash
go test -v
```
