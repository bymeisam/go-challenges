# Challenge 39: Struct Embedding

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Use struct embedding for composition (Go's alternative to inheritance).

## Tasks
1. Create `Base` struct with common fields
2. Embed Base in `Extended` struct
3. Access embedded fields directly

```bash
go test -v
```
