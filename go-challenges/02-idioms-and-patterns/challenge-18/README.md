# Challenge 18: Embedding

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Learn struct embedding - Go's composition over inheritance.

## Tasks
1. Define `Animal` struct with `Name` field and `Speak()` method
2. Embed `Animal` in `Dog` struct
3. Override `Speak()` method in `Dog`

```bash
go test -v
```
