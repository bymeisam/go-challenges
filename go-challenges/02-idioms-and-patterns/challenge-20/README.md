# Challenge 20: Stringer Interface

**Difficulty:** ⭐⭐ Medium | **Time:** 15 min

Implement the Stringer interface - Go's toString() equivalent.

## Tasks
1. Define `Person` struct with Name and Age
2. Implement `String() string` method
3. Use with `fmt.Println()` - it calls String() automatically!

```bash
go test -v
```
