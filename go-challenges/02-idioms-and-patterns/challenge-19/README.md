# Challenge 19: Type Assertions

**Difficulty:** ⭐⭐ Medium | **Time:** 15 min

Learn to safely extract concrete types from interfaces.

## Tasks
1. `GetString(v interface{}) (string, bool)` - type assertion with comma-ok
2. `TypeSwitch(v interface{}) string` - use type switch

```bash
go test -v
```
