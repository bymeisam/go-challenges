# Challenge 16: Table-Driven Tests

**Difficulty:** ⭐⭐ Medium | **Time:** 20 min

Master Go's table-driven testing pattern - the idiomatic way to write tests!

## Tasks
1. `Add(a, b int) int` - simple addition
2. Write table-driven tests with multiple test cases
3. Use subtests with `t.Run()`

```bash
go test -v
```
