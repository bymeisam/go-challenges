# Challenge 12: Multiple Return Values

**Difficulty:** ⭐⭐ Medium | **Time:** 15 min

## 🎯 Learning Goals
Master Go's idiom of returning multiple values (especially value + error)

## 🔨 Tasks
1. `ParseInt(s string) (int, bool)` - return value and success flag
2. `FindUser(id int) (*User, error)` - return pointer and error
3. `Split(s string) (first, last string)` - split name

Multiple returns are idiomatic in Go - embrace them!

```bash
go test -v
```
